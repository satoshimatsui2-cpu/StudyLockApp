<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/title_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:gravity="center"
        android:text="正しい順番に並べて下さい。"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textSize="13sp"
        android:textColor="#5A637A"
        android:alpha="0.90"
        tools:ignore="HardcodedText" />

    <!-- Japanese Translation -->
    <TextView
        android:id="@+id/japanese_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textColor="@color/sort_question_navy"
        tools:text="これはペンです。" />

    <!-- 正解英文表示エリア (Phase 3 修正) -->
    <LinearLayout
        android:id="@+id/correct_answer_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/bg_sort_correct_navy"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="12dp"
        android:visibility="gone"
        tools:visibility="visible">

        <TextView
            android:id="@+id/correct_answer_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            tools:text="This is the correct answer." />

        <ImageView
            android:id="@+id/icon_sort_tts"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginStart="8dp"
            android:src="@drawable/ic_round_play_circle_outline_24"
            app:tint="#FFFFFF"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="音声再生" />
    </LinearLayout>

    <!-- Answer Area (固定高さ + 中だけスクロール) -->
    <FrameLayout
        android:id="@+id/answers_box"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/shape_rounded_corners_gray"
        android:padding="8dp"
        android:clipToPadding="false"
        android:clipChildren="false">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true"
            android:overScrollMode="never"
            android:clipToPadding="false">

            <com.google.android.flexbox.FlexboxLayout
                android:id="@+id/flexbox_answers"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:alignContent="flex_start"
                app:flexWrap="wrap"
                app:justifyContent="flex_start"
                app:alignItems="flex_start">

                <!-- tools用：プレビュー確認のダミーチップ（実行時は removeAllViews() で消える想定） -->
                <com.google.android.material.chip.Chip
                    style="@style/Widget.MaterialComponents.Chip.Entry"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="This"
                    tools:ignore="MissingConstraints" />

            </com.google.android.flexbox.FlexboxLayout>
        </ScrollView>
    </FrameLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginVertical="8dp"
        android:background="?android:attr/listDivider" />

    <!-- Choices Area（ここは元のまま：weightで残す） -->
    <com.google.android.flexbox.FlexboxLayout
        android:id="@+id/flexbox_choices"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:alignContent="flex_start"
        app:flexWrap="wrap">

        <!-- tools用：プレビュー確認のダミーチップ -->
        <com.google.android.material.chip.Chip
            style="@style/Widget.MaterialComponents.Chip.Entry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="a"
            tools:ignore="MissingConstraints" />

        <com.google.android.material.chip.Chip
            style="@style/Widget.MaterialComponents.Chip.Entry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="is"
            tools:ignore="MissingConstraints" />

        <com.google.android.material.chip.Chip
            style="@style/Widget.MaterialComponents.Chip.Entry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="pen."
            tools:ignore="MissingConstraints" />

    </com.google.android.flexbox.FlexboxLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/button_sort_check"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="判定"
        android:enabled="false" />

</LinearLayout>
